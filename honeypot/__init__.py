"""Honey-AI CLI package — standalone tooling for operators.

Modules:
    validate_payloads — lint config/injection_payloads.yaml

Invoke via:
    python -m honeypot.validate_payloads
    python -m honeypot             # alias to validate_payloads
"""
